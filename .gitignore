# Existing legacy files
# (keep old static site for now)

# Next.js
/node_modules
/.next/
/out/
.DS_Store
*.tsbuildinfo
next-env.d.ts

# env
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# misc
.vercel
.playwright-mcp/

# graphify knowledge graph — a ~1MB local dev artifact the post-commit hook
# rebuilds on every commit. Tooling, not site content, and Cloudflare Pages
# would ship it to the public origin if it were tracked.
/graphify-out/
